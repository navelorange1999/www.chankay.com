{
  "name": "@chankay/wechat-relay",
  "private": true,
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "check-types": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "clean": "rm -rf dist && rm -rf .turbo"
  },
  "dependencies": {
    "@chankay/wechat-relay-protocol": "workspace:*"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "25.0.3",
    "vitest": "^3.2.4"
  }
}
