{
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["PAYLOAD_API_URL", "PAYLOAD_REVALIDATE_TIME"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "env": ["PAYLOAD_API_URL", "PAYLOAD_REVALIDATE_TIME"]
    }
  }
}
